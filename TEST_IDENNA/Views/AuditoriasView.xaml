<UserControl x:Class="TEST_IDENNA.Views.AuditoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewmodels="clr-namespace:TEST_IDENNA.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:AuditoriasViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{DynamicResource FondoPrincipal}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CargarDatosCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ShieldCheckOutline" Width="32" Height="32" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                <TextBlock Text="AUDITORÍA Y CONTROL DE SEGURIDAD" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextoPrincipal}" Margin="12,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Seguimiento estricto de acciones, control de integridad de datos y gestión de accesos al sistema." FontSize="14" Foreground="{DynamicResource TextoSecundario}" Margin="44,4,0,0"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" Padding="20" Margin="0,0,0,20" Background="{DynamicResource FondoTarjetas}" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding BusquedaTexto, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Buscar por Usuario o Descripción..." 
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,20,0"
                         Foreground="{DynamicResource TextoPrincipal}"/>

                <ComboBox Grid.Column="1" ItemsSource="{Binding ModulosDisponibles}" 
                          SelectedItem="{Binding ModuloSeleccionado}"
                          materialDesign:HintAssist.Hint="Módulo / Pantalla" 
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,20,0"
                          Foreground="{DynamicResource TextoPrincipal}"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <DatePicker Width="120" SelectedDate="{Binding FechaInicio}" 
                                Foreground="{DynamicResource TextoPrincipal}"/>
                    <TextBlock Text="al" Margin="10,0,10,5" VerticalAlignment="Bottom" Foreground="{DynamicResource TextoSecundario}"/>
                    <DatePicker Width="120" SelectedDate="{Binding FechaFin}" 
                                Foreground="{DynamicResource TextoPrincipal}"/>
                </StackPanel>

                <Button Grid.Column="3" Command="{Binding CargarDatosCommand}" 
                        Style="{StaticResource MaterialDesignIconButton}" ToolTip="Refrescar Datos" VerticalAlignment="Bottom">
                    <materialDesign:PackIcon Kind="Refresh" Width="24" Height="24"/>
                </Button>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Background="{DynamicResource FondoTarjetas}" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
            <DataGrid ItemsSource="{Binding ListaAuditorias}" AutoGenerateColumns="False" IsReadOnly="True"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      Background="Transparent" HeadersVisibility="Column">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                        <Setter Property="Foreground" Value="{DynamicResource TextoSecundario}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <materialDesign:PackIcon Width="20" Height="20" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Accion}" Value="CREAR">
                                                    <Setter Property="Kind" Value="PlusCircleOutline" />
                                                    <Setter Property="Foreground" Value="#22c55e" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Accion}" Value="MODIFICAR">
                                                    <Setter Property="Kind" Value="PencilCircleOutline" />
                                                    <Setter Property="Foreground" Value="#f59e0b" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Accion}" Value="ELIMINAR">
                                                    <Setter Property="Kind" Value="MinusCircleOutline" />
                                                    <Setter Property="Foreground" Value="#ef4444" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Fecha y Hora" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy hh:mm tt'}" Width="160" FontWeight="SemiBold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="Foreground" Value="{DynamicResource TextoPrincipal}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Usuario" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="UserOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextoPrincipal}"/>
                                    <TextBlock Text="{Binding Usuario}" VerticalAlignment="Center" FontWeight="Medium" Foreground="{DynamicResource TextoPrincipal}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Módulo" Binding="{Binding Modulo}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="Foreground" Value="{DynamicResource TextoPrincipal}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Detalles del Movimiento" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Detalles}" TextWrapping="Wrap" FontSize="12" Opacity="0.8" Foreground="{DynamicResource TextoPrincipal}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Seguridad" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialDesign:Chip Content="{Binding Rol}" Background="#dcfce7" Foreground="#166534" Height="24" FontSize="10"/>
                        </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>