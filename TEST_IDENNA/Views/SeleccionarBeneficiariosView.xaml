<Window x:Class="TEST_IDENNA.Views.SeleccionarBeneficiariosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:TEST_IDENNA.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SeleccionarBeneficiariosViewModel}"
        mc:Ignorable="d"
        Title="Asignar Beneficiarios al Documento" 
        Height="600" Width="680"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource FondoPrincipal}">

    <Grid Margin="20" Background="{DynamicResource FondoPrincipal}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Vincular Beneficiarios" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextoPrincipal}" Margin="0,0,0,2"/>
            <TextBlock Text="Escriba el nombre, apellido o cédula para filtrar. Las selecciones permanecerán fijadas." FontSize="12" Foreground="{DynamicResource TextoSecundario}" Margin="0,0,0,10"/>

            <TextBox Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="14"
                     Padding="8,6"
                     BorderThickness="1"
                     BorderBrush="#CCCCCC"
                     Foreground="{DynamicResource TextoPrincipal}"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Beneficiarios Seleccionados (Fijados)" Margin="0,5,0,15" Padding="6" Background="{DynamicResource FondoTarjetas}" BorderBrush="#E0E0E0">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="90">
                <ItemsControl ItemsSource="{Binding SeleccionadosFijados}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#E1F5FE" BorderBrush="#0288D1" BorderThickness="1" CornerRadius="12" Margin="3" Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" Foreground="#01579B">
                                        <Run Text="{Binding Nombres}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Apellidos}"/>
                                        <Run Text=" - C.I: "/>
                                        <Run Text="{Binding Cedula}"/>
                                    </TextBlock>

                                    <Button Content="✕" 
                                            Command="{Binding DataContext.QuitarFijadoCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent" BorderThickness="0" Foreground="#D32F2F" 
                                            Margin="6,0,0,0" Padding="0" Cursor="Hand" FontWeight="Bold" FontSize="11"
                                            VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <Border Grid.Row="2" Background="{DynamicResource FondoTarjetas}" BorderBrush="{DynamicResource FondoTarjetas}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,15">
            <DataGrid ItemsSource="{Binding Items}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource FondoAvatarDefecto}"
                      RowHeight="38"
                      Background="{DynamicResource FondoTarjetas}"
                      BorderThickness="0"
                      HeadersVisibility="Column">

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource FondoFilaSeleccionada}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource FondoTarjetas}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextoSecundario}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource FondoTarjetas}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="80" Header="Selección">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Cédula" Binding="{Binding Beneficiario.Cedula}" Width="110" IsReadOnly="True" Foreground="{DynamicResource TextoPrincipal}"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Beneficiario.Nombres}" Width="160" IsReadOnly="True" Foreground="{DynamicResource TextoPrincipal}"/>
                    <DataGridTextColumn Header="Apellido" Binding="{Binding Beneficiario.Apellidos}" Width="*" IsReadOnly="True" Foreground="{DynamicResource TextoPrincipal}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Click="BotonCancelar_Click" Width="100" Height="32" Margin="0,0,10,0" Background="{DynamicResource FondoTarjetas}" BorderThickness="0" Cursor="Hand" Foreground="{DynamicResource TextoPrincipal}"/>
            <Button Content="Confirmar Selección" Click="BotonAceptar_Click" Width="150" Height="32" Background="#007ACC" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>